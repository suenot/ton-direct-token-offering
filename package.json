{
  "name": "ton-mm-exchange",
  "version": "1.0.0",
  "description": "TON contract for exchanging USDT for MM tokens",
  "main": "deploy.js",
  "scripts": {
    "build": "npx func-js mm_exchange.fc --boc mm_exchange.cell",
    "compile": "tsc",
    "deploy": "npm run build && npm run compile && node dist/deploy.js"
  },
  "dependencies": {
    "@ton/ton": "^13.9.0",
    "dotenv": "^16.0.3",
    "ton-core": "^0.49.1",
    "ton-crypto": "^3.2.0",
    "@orbs-network/ton-access": "^2.3.3"
  },
  "devDependencies": {
    "@ton-community/func-js": "^0.6.2",
    "@types/node": "^18.11.9",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.3"
  }
}
